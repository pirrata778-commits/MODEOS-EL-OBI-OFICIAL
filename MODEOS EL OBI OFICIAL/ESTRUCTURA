EL MODEOS EL OBI OFICIAL/
├── .env                     # Variables de entorno (tokens, BD, puertos)
├── package.json             # Dependencias y scripts de inicio (npm start)
│
├── server/
│   ├── config.ts            # 1. Configuración global (Discord, DB, Express)
│   │
│   ├── db/
│   │   ├── index.ts         # Conexión con PostgreSQL (Drizzle ORM)
│   │   └── schema.ts        # Tablas (warns, blacklist, user_sessions, detected_admins)
│   │
│   ├── bot/
│   │   ├── index.ts         # 2. BOT PRINCIPAL (Cliente discord.js, eventos y comandos slash)
│   │   ├── adminScanner.ts  # Módulo de escaneo e inserción para 『𝑨𝑫𝑴𝑰𝑵 𝑫𝑬𝑻𝑬𝑪𝑻𝑨𝑫𝑶 ⚠️』
│   │   └── statsChannels.ts # Módulo nuevo: Canales de voz de estadísticas automáticas (Bots, Usuarios, Admins, Total)
│   │
│   ├── routes/
│   │   ├── api.ts           # 3. Rutas API para la Dashboard (status, logs, admins detectados)
│   │   └── auth.ts          # Autenticación y gestión de sesiones
│   │
│   └── index.ts             # 4. SERVIDOR CENTRAL (Levanta Express + Bot + Sirve Frontend)
│
└── client/                  # (O src/) Frontend React + Shadcn UI del Dashboard
    ├── src/
    │   ├── pages/           # Páginas del panel (Admin, Blacklist, Vouches, Admins Detectados)
    │   └── components/      # Componentes UI
    └── index.html